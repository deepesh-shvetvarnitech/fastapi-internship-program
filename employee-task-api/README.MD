# Employee Task API

A simple FastAPI backend service for employee and task management.

## Setup

Create and activate the virtual environment.

## Install dependencies

```bash
pip install -r requirements.txt